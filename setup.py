from setuptools import setup

setup(name='domain_rand',
      version='0.0.1',
      install_requires=['gymnasium>=0.27.1', 'mujoco>=2.3.1.post1', "torch", "scipy"]  # And any other dependencies foo needs
)
